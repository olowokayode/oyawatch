# Only needed if you switch index.html to API_MODE = 'proxy'
# (moves the TMDB key server-side via api/tmdb.js or the Netlify function).
# Set this in your Vercel/Netlify project settings, NOT committed to git.
TMDB_KEY=your_tmdb_v3_api_key_here
